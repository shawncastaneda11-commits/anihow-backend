# Reviews

Buyer ratings of a farmer-seller after a **completed** reservation. One review per reservation.

**Implemented**

- `POST /api/buyer/reviews`
- `GET /api/buyer/shops/{farmerSeller}/reviews` — list + average rating
